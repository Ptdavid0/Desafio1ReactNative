import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  noTaskContainer: {
    justifyContent: "center",
    alignItems: "center",
    marginTop: 136,
  },
  noTaskUpperText: {
    color: "#656565",
    fontSize: 16,
    fontWeight: "bold",
  },
  noTaskLowerText: {
    color: "#656565",
    fontSize: 17,
  },
  noTaskImage: {
    resizeMode: "contain",
    width: 60,
    height: 60,
    opacity: 0.4,
    marginBottom: 16,
  },
});
